management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: never

spring:
  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    com.enterprise.aiassistant: INFO
    dev.langchain4j: WARN
